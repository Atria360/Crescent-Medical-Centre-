export const dynamic = "force-dynamic";

export const metadata = { title: "Admin – Crescent Medical Centre" };

export default function AdminRootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
